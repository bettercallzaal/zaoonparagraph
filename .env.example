# Paragraph "AI Assistant" API key. Settings > Workspace > Developer on paragraph.com.
# Scoped to the @thezao publication. Never commit the real value - this file is gitignored (.env, .env.*).
PARAGRAPH_API_KEY=
